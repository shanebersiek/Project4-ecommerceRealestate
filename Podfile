# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'NOTES-EcommerceApp' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for NOTES-EcommerceApp
   pod 'Firebase/Core'
   pod 'Firebase/Database'
   pod 'Firebase/Auth'
   pod 'Firebase/Storage'
   pod 'Backendless'
   pod 'OneSignal'
   pod 'ImagePicker'
   pod 'IDMPhotoBrowser'
   
   
  target 'NOTES-EcommerceAppTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'NOTES-EcommerceAppUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
